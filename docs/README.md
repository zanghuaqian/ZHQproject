# 采购商城订单管理（静态演示页）

本目录由仓库根目录的 `procurement-mall-order-admin.html` 同步生成，用于 **GitHub Pages** 托管。

- 访问根路径即可打开页面（`index.html`）。
- 更新演示页后，请将根目录 `procurement-mall-order-admin.html` 覆盖复制到本目录的 `index.html`。
