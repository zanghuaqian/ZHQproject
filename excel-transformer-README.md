# Excel Transformer Skill

一个强大的Excel文档转换生成器技能，支持根据用户上传的Excel文档或本地文件夹中的xlsx文件，以及指定的要求、格式规范，生成符合要求的新Excel文档。

## 功能特性

- ✅ 支持读取本地文件夹中的指定xlsx文件
- ✅ 支持数据转换、格式设置、公式计算
- ✅ 支持多表合并、数据透视
- ✅ 特别支持生成"盛意旺采购商城对账单"标准格式
- ✅ 自动数据完整性检查
- ✅ 智能字段映射
- ✅ 自动公式计算和格式设置
- ✅ 金额空值自动填入 0.00
- ✅ 跨月退款订单自动识别和标注（#DDEBF7背景色）
- ✅ 统一字体格式（等线）
- ✅ **跨月退订单间隔显示**（v1.2）：跨月退款与普通订单间隔两行
- ✅ **跨月退订单红色标识**（v1.2）：月份和收款时间列显示为红色
- ✅ **智能收款时间提取**（v1.2）：从订单号自动提取收款时间
- ✅ **表格末尾统计行**（v1.2）：自动统计收款金额、退款-退货、钱包款

## 安装

将 `excel-transformer.skill` 文件复制到你的技能目录即可使用。

## 使用方法

### 基本用法

用户只需提供：
1. Excel文件路径（本地文件路径）
2. 输出格式要求

技能会自动：
- 读取和分析文件
- 映射字段
- 生成符合要求的Excel文档

### 对账单生成

使用提供的脚本快速生成对账单：

```bash
python scripts/generate_statement.py <原数据文件> <交易对账单文件> <月份> [输出文件]
```

示例：
```bash
python scripts/generate_statement.py data.xlsx reference.xlsx 1
```

## 文件结构

```
excel-transformer/
├── SKILL.md                    # 主技能文档
├── scripts/
│   └── generate_statement.py   # 对账单生成脚本
└── references/
    ├── common-patterns.md      # 常见转换模式
    ├── file-reading.md         # 文件读取指南
    ├── statement-format.md     # 对账单格式规范
    └── statement-mapping.md    # 对账单字段映射规则
```

## 依赖

- pandas
- openpyxl

安装依赖：
```bash
pip install pandas openpyxl
```

## 许可证

请查看技能包中的LICENSE文件（如果有）。

## 更新日志

### v1.3.0 (2026-01-30)
- 🔧 修复跨月退订单收款金额显示问题：收款金额正确显示为0.00
- 🔧 修复跨月退订单钱包款格式问题：负数正确显示为红色括号
- 🔧 修复空行格式问题：空行不再填充任何数值和公式
- ✨ 优化跨月退款金额计算逻辑：
  - 从交易对账单的"订单金额(元)"字段获取退款金额
  - 根据订单金额与快递费、实付款的关系智能判断退款类型
  - 仅退快递费：退款-快递费填充快递费，退款-退货为0
  - 退快递费和退货：退款-快递费填充快递费，退款-退货填充实付款
- ✨ 退款字段优化：退款-快递费和退款-退货始终显示为正数

### v1.2.0 (2026-01-28)
- ➕ 跨月退订单与普通订单间隔两行
- ➕ 跨月退订单首列和收款时间列显示为红色
- ➕ 跨月退收款时间从订单号自动提取（第3-10位YYYYMMDD）
- ➕ 表格末尾添加统计行（收款金额、退款-退货、钱包款）
- 🎨 统计行使用微软雅黑 16号 加粗字体

### v1.1.0 (2026-01-28)
- 金额空值自动填入 0.00
- 跨月退款订单自动识别和处理
- 跨月退款订单用 #DDEBF7 背景色标识
- 表头格式优化：无背景色，黑色加粗文字
- 全表统一使用等线字体

### v1.0.0 (2026-01-27)
- 初始版本
- 支持基础Excel转换功能
- 支持盛意旺采购商城对账单生成
- 完整的字段映射规则
- 自动退款处理
- 支付渠道自动获取
