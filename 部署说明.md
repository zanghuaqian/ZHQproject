# 静态页面部署说明（GitHub Pages / 本地）

本仓库含多个独立的 **HTML** 页面。GitHub Pages 常见两种发布方式，对应路径不同：

| 发布来源（Settings → Pages → Folder） | 站点根指向 | 示例 |
|------------------------------------------|------------|------|
| **/docs**（本仓库常用） | 仓库里的 `docs/` | `product-list.html` 必须存在于 **`docs/product-list.html`**，线上为 `https://用户名.github.io/仓库名/product-list.html` |
| **/ (root)** | 仓库根目录 | `product-list.html` 放在仓库根即可 |

若在 **docs 发布** 下，`docs` 文件夹外的页面不会上线，易出现 **404**；请把需要在 Pages 打开的页面放进 **`docs/`**（或与根目录保持一致地复制一份），再 `push`。

**docs 站内导航：** 发布后也可打开 `https://用户名.github.io/仓库名/pages-nav.html`。

---

## GitHub Pages 开启步骤

1. **Settings → Pages**
2. **Source**：Deploy from a branch → **Branch `main`**，文件夹选 **`/docs`** 或 **`/ (root)`**（按需）
3. `.nojekyll`：请在**实际发布的根**下放一份（仓库根已有；若只用 `docs/`，请在 `docs/` 下保留 `.nojekyll`，本仓库已包含）

---

## 访问示例（用户名与仓库名请替换为你的）

假设 Pages 来源为 **`/docs`**、`docs` 内已有对应文件：

- `https://zanghuaqian.github.io/ZHQproject/product-list.html`
- `https://zanghuaqian.github.io/ZHQproject/pages-nav.html`
- `https://zanghuaqian.github.io/ZHQproject/` → 通常为 `docs/index.html`（订单管理）

---

## 本地预览

```bash
npm start
# 或
npx serve -l 3000
```

仓库根：`http://localhost:3000/` 及 `product-list.html` 等均可测。  
若以 `docs` 为准，也可用 `cd docs` 后对当前目录起一个静态服务，模拟 Pages。

---

## 新增页面并在 Pages 生效

**发布源为 `/docs` 时：**

1. 在 **`docs/`** 下放置或复制 `*.html`。  
2. `git add` / `push`。  
3. 到 **`docs/pages-nav.html`** 或仓库根的 **`index.html`** 添加链接。

**发布源为根目录时：** 把文件放在仓库根（或拷贝到 docs 视你的策略而定），并更新根的 **`index.html`**。

---

## 与根目录 `product-list.html` 保持一致

若以 **docs** 为发布源，`docs/product-list.html` 应与根目录 **`product-list.html`** 保持一致；本地改完原型后请将根目录版本 **复制覆盖** `docs/product-list.html` 后再提交推送。
