# 商品列表页面部署说明

入口文件为仓库根目录 **`index.html`**（使用 CDN，无需构建）。

## GitHub Pages（公网访问）

**预览地址（启用 Pages 后）：**  
**https://zanghuaqian.github.io/ZHQproject/**  
或：**https://zanghuaqian.github.io/ZHQproject/index.html**

### 在 GitHub 仓库中开启

1. 打开仓库 **Settings（设置）** → 左侧 **Pages（页面）**
2. **Build and deployment**：Source 选 **Deploy from a branch**
3. **Branch** 选 **main**，文件夹选 **/ (root)**，保存
4. 等待 1～2 分钟，上方会显示绿色提示与站点 URL（若未显示，刷新页面）

> 若页面空白，确认已推送最新提交（含 `index.html`）。本仓库根目录已包含 **`.nojekyll`**，避免部分静态资源被 Jekyll 错误处理。

---

## 方式一：本地启动（供局域网访问）

在项目目录下执行：

```bash
npm start
```

或：

```bash
npx serve -l 3000
```

启动后：

- **本机访问**：http://localhost:3000/ 或 http://localhost:3000/index.html
- **局域网他人访问**：http://你的电脑IP:3000/  
  （在命令行执行 `ipconfig` 查看本机 IP，如 192.168.1.100）

## 方式二：其他静态托管

将 **`index.html`** 与 **`.nojekyll`**（若目标平台需要）一并部署到 **Vercel**、**Netlify** 等即可。

## 方式三：其他 HTTP 服务

- **Python**：`python -m http.server 3000`（在项目目录执行）
- **Node**：`npx serve -l 3000`

访问：http://localhost:3000/
