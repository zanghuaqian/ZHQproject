# 静态页面部署说明（GitHub Pages / 本地）

本仓库包含多个独立 **`.html`** 页面，均可单独通过 URL 访问，互不替换。

## GitHub Pages

开启方式：**仓库 Settings → Pages → Branch `main` / 根目录 `/`**。

### 站点根地址

- **导航首页（入口目录）**：`https://zanghuaqian.github.io/ZHQproject/`  
  对应根目录 **`index.html`**，用于列出各页面链接。

### 各页面独立访问示例

在浏览器地址栏使用「根地址 + 相对路径」即可，例如：

| 页面说明 | 访问路径（接在域名后） |
|----------|------------------------|
| 采购商城原型（商品列表等） | `/product-list.html` |
| 采购商城 · 订单管理 | `/procurement-mall-order-admin.html` |
| 确认订单 | `/confirm-order.html` |
| 新增商品 | `/add-product-form.html` |
| docs 下订单管理 | `/docs/index.html` |
| docs 下确认订单 | `/docs/confirm-order.html` |

完整示例：

- `https://zanghuaqian.github.io/ZHQproject/product-list.html`

> **注意**：若某 HTML 从未提交到 GitHub，Pages 上无法打开，需先将该文件 `git add` 并推送。

仓库根目录已包含 **`.nojekyll`**，减少 Jekyll 对静态资源的干扰。

---

## 本地预览

在项目根目录执行：

```bash
npm start
```

或：`npx serve -l 3000`

- 导航页：<http://localhost:3000/>
- 单独页面：<http://localhost:3000/product-list.html> 等。

依赖 CDN 的页面**无需** `npm install` 也可直接打开 HTML 文件（建议仍用 HTTP 服务以避免部分浏览器对 `file://` 的限制）。

---

## 新增独立页面

1. 在仓库中新增或保留你的 `xxx.html`（或子目录下的页面）。  
2. 使用 `git add` / `push` 推送到 GitHub。  
3. 到根目录 **`index.html`（导航页）** 中按相同格式增加一条链接，方便从首页跳转。
