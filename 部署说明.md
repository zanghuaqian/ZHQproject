# 商品列表页面部署说明

## 方式一：本地启动（推荐，供局域网访问）

在项目目录下执行：

```bash
npm start
```

或：

```bash
npx serve -l 3000
```

启动后：
- **本机访问**：http://localhost:3000/product-list.html
- **局域网他人访问**：http://你的IP:3000/product-list.html  
  （在命令行执行 `ipconfig` 查看本机 IP，如 192.168.1.100）

## 方式二：静态文件部署

页面已改为使用 CDN 依赖，无需 `node_modules`。可将以下文件部署到任意静态托管：

- `product-list.html`

部署到 **GitHub Pages**、**Vercel**、**Netlify** 等后，他人可通过公网链接访问。

## 方式三：其他 HTTP 服务

- **Python**：`python -m http.server 3000`（在项目目录执行）
- **Node**：`npx serve -l 3000`

访问地址同上。
